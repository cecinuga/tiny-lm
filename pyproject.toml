[project]
name = "tiny-lm"
version = "0.1.0"
description = "Train a GPT from scratch on a HP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0.2",
    "torch>=2.8.0",
    "tqdm>=4.67.3",
]
